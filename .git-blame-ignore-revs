# Commits that only moved whitespace and line breaks, skipped by git blame so a
# line is credited to whatever last changed its meaning.
#
# GitHub reads this file on its own. Locally it takes one setting:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Entries are full SHAs and die silently if these commits are ever rebased.

# Adopt the standard Rust formatting
2b3eed92a9ffc9421cc4b17797c4c416ec67d43f
